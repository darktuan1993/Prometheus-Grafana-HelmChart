groups:
  - name: Rules
    rules:
      # Node Down
      - alert: Cảnh Báo Server
        expr:  up{instance=~".+"} == 0
        for: 1m
        labels:
          severity: Khẩn cấp
          app_type: ClusterManager
          category: server
          instance: "{{ $labels.instance }}"
        annotations:
          summary: "Exporter trên server {{ $labels.instance }} bị tắt"
          description: | 
                Vui lòng kiểm tra lại service giám sát trên server?