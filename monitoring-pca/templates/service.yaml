apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitoring-pca.fullname" . }}
  labels:
    {{- include "monitoring-pca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitoring-pca.selectorLabels" . | nindent 4 }}
