apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-grafana-helmchart.fullname" . }}
  labels:
    {{- include "prometheus-grafana-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-grafana-helmchart.selectorLabels" . | nindent 4 }}
