apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-grafana-helmchart.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-grafana-helmchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-grafana-helmchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
